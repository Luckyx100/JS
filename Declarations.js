console.log("Hi Lucky, Welcome to Javascript world !");

let name = "Lucky Kumar";  // String data type
name = "KL University";    // Updates value to other one
console.log(name);

const laptop = "Lenovo";   // Constant variable
console.log(laptop);

let active = true;  // A boolean data type 

let distance = 5478;   // Integer Data type & for big integer type " 5478n "

let temp = null;   // Some space in Memory is allocated, but no data (or) Similar to 0.

let Humans_Inside_Sun = undefined;   // No mermory allocation for undefined data type.